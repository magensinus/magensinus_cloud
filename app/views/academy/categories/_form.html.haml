= form_for @academy_category do |f|
  - if @academy_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@academy_category.errors.count, "error")} prohibited this academy_category from being saved:"
      %ul
        - @academy_category.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :meta_title
    = f.text_field :meta_title
  .field
    = f.label :meta_description
    = f.text_area :meta_description
  .field
    = f.label :meta_image_box
    = f.text_field :meta_image_box
  .field
    = f.label :meta_url
    = f.text_field :meta_url
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description

  .field
    = f.label :thumb_box
    = f.hidden_field :thumb_box_cache
    - if @academy_category.thumb_box.presence
      = f.hidden_field :thumb, value: true
    - else
      = f.hidden_field :thumb, value: false
    = f.file_field :thumb_box unless @academy_category.thumb_box.presence
  - if @academy_category.thumb_box.presence
    .field
      = f.label :remove_thumb_box
      = f.check_box :remove_thumb_box
  .field
    = f.label :thumb_caption
    = f.text_field :thumb_caption

  .field
    = f.label :cover_box
    = f.hidden_field :cover_box_cache
    - if @academy_category.cover_box.presence
      = f.hidden_field :cover, value: true
    - else
      = f.hidden_field :cover, value: false
    = f.file_field :cover_box unless @academy_category.cover_box.presence
  - if @academy_category.cover_box.presence
    .field
      = f.label :remove_cover_box
      = f.check_box :remove_cover_box
  .field
    = f.label :cover_caption
    = f.text_field :cover_caption

  .field
    = f.label :published
    = f.check_box :published
  .field
    = f.label :eml
    = f.check_box :eml
  .field
    = f.label :magestil
    = f.check_box :magestil
  .field
    = f.label :magensinus
    = f.check_box :magensinus
  .actions
    = f.submit 'Save'

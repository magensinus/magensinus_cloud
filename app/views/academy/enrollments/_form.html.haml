= form_for [@academy_category, @academy_enrollment], url: academy_enrollment_form_url do |f|
  = hidden_field :academy_category_id, value: @academy_category.id
  .field
    = f.label :email
    = f.text_field :email
  .actions
    = f.submit "Save"

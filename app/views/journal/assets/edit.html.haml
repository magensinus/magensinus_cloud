%h1 Editing journal_asset

- if params[:text] == "true"
  = render 'form_text'
- if params[:image] == "true"
  = render 'form_image'
- if params[:video] == "true"
  = render 'form_video'

= link_to 'Show', journal_article_asset_path(@journal_article, @journal_asset)
\|
= link_to 'Back', journal_article_assets_path(@journal_article)

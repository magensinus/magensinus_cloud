// Show

.container-top
  = render "lib/components/breadcrumbs", path: magestil_dashboard_path
.container-middle.left
  .board-double-extra-small
    = render partial: "secondary_navigation"
  .board-medium
    // - if params_image
    //   = render partial: "image", locals: { object: @magestil_courses }
    // - else
    = render partial: "form", locals: { object: @magestil_courses }
.container-bottom.left

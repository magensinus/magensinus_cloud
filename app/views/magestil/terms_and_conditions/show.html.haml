// Show

.container-top
  .breadcrumbs
    .breadcrumbs-box
      .breadcrumbs-item
        = link_to magestil_dashboard_path, class: "link medium gray" do
          .icon.left
            = image_tag "chevron_left.png"
          Back
.container-middle.left
  .board-double-extra-small
    = render partial: "secondary_navigation"
  .board-medium
    // - if params_image
    //   = render partial: "image", locals: { object: @magestil_terms_and_conditions }
    // - else
    = render partial: "form", locals: { object: @magestil_terms_and_conditions }
.container-bottom.left

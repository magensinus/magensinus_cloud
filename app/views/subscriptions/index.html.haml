%h1 Listing items

%table
  %thead
    %tr
      %th Active
      %th Email
      %th
      %th
      %th

  %tbody
    - @subscriptions.each do |item|
      %tr
        %td= item.active
        %td= item.email
        %td= link_to 'Show', item
        %td= link_to 'Edit', edit_subscription_path(item)
        %td= link_to 'Destroy', subscription_path(item), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Subscription email', new_subscription_path
\|
= link_to 'Dashboard', root_path
